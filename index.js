//CHALLENGE 1: Escribe una función sayHello() que regrese el string "hello"

function sayHello() {
  return "hello";
}

//CHALLENGE 2: Escribe una función suma() que regrese la suma de 2 numeros dados.

function suma(a, b) {
  return a + b;
}
